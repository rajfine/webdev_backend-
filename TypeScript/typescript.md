cmd's

1. npm init -y
2. npm i typscript  --save-dev
3. npx tsc --init
4. touch index.ts  // creating file 
5. npx tsc

* Do -  in (json file)
type: "module"
target: "esnext"

//change into js file and run that
* npx tsc index.ts --ignoreConfig && node index.js